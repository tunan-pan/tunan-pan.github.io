---
layout: base.njk
title: Projects
---

<div class="projects-intro">
  <h1>My Projects</h1>
  <p>This page is still a work in progress and being updated regularly. Images and case studies will be coming soon!</p>
</div>

<!-- DEBUG: Remove this after checking 
  <details>
    <summary>Debug: Click to see project data</summary>
    <pre style="background: #f5f5f5; padding: 1rem; overflow: auto;">
{{ projects_build | dump(2) }}
    </pre>
  </details>
</div>-->


  
<div class="projects-grid">

  {% for project in projects_build %}
    <article class="project-item">

      {% if project.image %}
        <div class="project-image">
          <a href="/projects/{{ project.slug }}/" tabindex="-1" aria-hidden="true">
            <img src="{{ project.image.url or project.image }}" alt="">
          </a>
        </div>
      {% endif %}

      {% if project.category %}
        <span class="project-categories">
          {% if project.category is iterable and project.category is not string %}
            {% for cat in project.category %}
              <a href="/categories/{{ cat | slug }}/">{{ cat }}</a>{% if not loop.last %}, {% endif %}
            {% endfor %}
          {% else %}
            <a href="/categories/{{ project.category | slug }}/">{{ project.category }}</a>
          {% endif %}
        </span>
      {% endif %}

      <h2><a href="/projects/{{ project.slug }}/">{{ project.title }}</a></h2>

      {% if project.tags %}
        <span class="project-tags">
          {% for tag in project.tags %}
            <a href="/tags/{{ tag | slug }}/">{{ tag }}</a>{% if not loop.last %}, {% endif %}
          {% endfor %}
        </span>
      {% endif %}

    </article>
  {% endfor %}

</div>